module healing_humanity::test_helpers {
    use sui::object::{UID, object};
    use sui::tx_context::TxContext;

    struct MockOracle has key {
        id: UID,
    }

    public fun create(ctx: &mut TxContext): MockOracle {
        MockOracle {
            id: object::new(ctx),
        }
    }
}
